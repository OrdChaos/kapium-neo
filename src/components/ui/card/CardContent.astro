---
import { cn } from '@/lib/utils';

interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className, ...rest } = Astro.props;
---

<div data-slot="card-content" class={cn('px-6 w-full overflow-hidden', className)} {...rest}>
  <slot />
</div>
